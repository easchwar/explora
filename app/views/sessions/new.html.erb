<%= render 'shared/landing', user: @user, url: session_url, button_text: "Sign In", type: "session" %>

<script charset="utf-8">
  $(function() {
    $(".button-guest").on("click", function() {
      $(".sign-in-username").val("ExploraNinja")
      $(".sign-in-password").val("exploraninja")
      $("form").submit()
    })
  });
</script>
